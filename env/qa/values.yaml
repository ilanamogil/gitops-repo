# QA environment overrides for flask-aws-monitor
replicaCount: 2

image:
  tag: qa

service:
  type: ClusterIP

ingress:
  enabled: true
  host: flask-monitor.qa.local

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
